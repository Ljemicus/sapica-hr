# Supabase Setup Instructions

## Run these SQL files in order in the Supabase SQL Editor:

1. Go to https://supabase.com/dashboard/project/hmtlcgjcxhjecsbmmxol/sql
2. Run the contents of `migrations/005_lost_pets.sql`
3. Run the contents of `seed_lost_pets.sql`

## Or run the combined file:
Copy and paste the contents of `run_all.sql` into the SQL Editor and execute.
